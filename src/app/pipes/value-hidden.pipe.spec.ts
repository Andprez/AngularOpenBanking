import { ValueHiddenPipe } from './value-hidden.pipe';

describe('ValueHiddenPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueHiddenPipe();
    expect(pipe).toBeTruthy();
  });
});
